#/bin/sed -f 有什么用处？
sed 和grep ,awk有什么区别？
xargs的用途
exec的用途
makefile的使用

这怎么解释？
a=2234
b=${a/23/BB}
echo $b


大于9的参数必须加大括号
${10}
!在命令行下要注意，当作了历史命令
\有续行的作用要注意

"foo\
bar" 弱引用下，\将有续行作用

’foo\
bar'没有续行作用

（（算术测试））

IFS存放默认分隔符

$*认为所用参数为一个单词
$@认为每个参数为一个单词